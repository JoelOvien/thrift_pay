import 'package:flutter/material.dart';

class AppColors {
  // ignore: prefer_const_constructors
  var activeColor = Color(0xfff89900);
  var whiteColor = Colors.white;
  var blackColor = Colors.black;
  var redColor = Colors.red;
  var greyColor = Colors.grey;
  var blueColor = Colors.blue;
  var transparentColor = Colors.transparent;
  // ignore: prefer_const_constructors
  var primaryColor = Color(0xff2738D2);
  // ignore: prefer_const_constructors
  var darkBluePrimary = Color(0xff0C1141);
}

AppColors colors = AppColors();
